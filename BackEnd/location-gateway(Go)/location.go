package main

/*
Legacy placeholder types kept for reference.

// GeoLocation struct defines the data stored for a user's location.
// Note: In a real system, this would likely be stored in a PostGIS database.
type GeoLocation struct {
	LocationID int64   `json:"id"`
	UserID     int64   `json:"userId"`
	Latitude   float64 `json:"latitude"`
	Longitude  float64 `json:"longitude"`
	Address    string  `json:"address"`
}

// NearbyItemResult is a DTO (Data Transfer Object) for the proximity search
// It combines the item ID with the calculated distance.
type NearbyItemResult struct {
	FoodID     int64   `json:"foodId"`
	ProviderID int64   `json:"providerId"`
	DistanceKM float64 `json:"distanceKm"`
}
*/